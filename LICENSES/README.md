# LICENSES

このディレクトリには、プロジェクトが使用するサードパーティライブラリのライセンスファイルを格納します。

## ファイル命名規則

`{PackageName}.txt` 形式で配置してください。

## 追加手順

新しい依存パッケージを追加する際は：

1. このディレクトリにライセンスファイルを追加（`{PackageName}.txt` 形式）
2. `README.md` のサードパーティライセンスセクションに著作権表示を追加
